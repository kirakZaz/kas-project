export const styles = {
    root: {
        position: 'relative',
        minHeight: '100vh',
        bgcolor: 'background.default',
    } satisfies any,

    main: {
        position: 'relative',
        zIndex: 1,
    } satisfies any,
}
